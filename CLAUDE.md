# CLAUDE.md

本文件为 Claude Code 在此仓库中工作时必须遵循的项目级指令。

---

## 提交前检查

每次提交之前，**必须**确认 `README.md` 中的信息与仓库当前最新状态保持同步。如果 README 中的目录结构、文件列表、规则数量、安装方式等信息与实际情况不一致，必须在提交前先更新 README。

---

## 提交确认

**不得**在用户未明确同意的情况下自动执行 `git commit` 或 `git push`。每次准备提交时，必须先将修改内容摘要展示给用户，等待用户确认后方可执行提交和推送操作。
