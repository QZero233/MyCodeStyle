# Git Commit 风格规范

## 1. Commit Message 格式

提交信息的第一行（subject）必须遵循以下格式：

```
<type>(<scope>): <description>
```

其中 `description` 部分必须使用**中文**来写。

### type 常用取值

- `feat`: 新功能
- `fix`: 修复 bug
- `refactor`: 重构（既非新功能也非修 bug）
- `docs`: 文档变更
- `style`: 代码格式调整（不影响逻辑）
- `test`: 测试相关
- `chore`: 构建/工具/依赖等杂项

### scope

用简短的英文标识本次变更影响的模块或领域。

---

## 2. Commit Body 要求

提交信息的主体部分必须写清楚以下两点：

### 2.1 本次修改的主要内容，要解决的问题是什么

说明为什么要做这次提交，背景是什么，解决了什么问题或实现了什么需求。

### 2.2 每个文件都做了哪些修改，为什么做这些修改

逐文件列出变更内容，并解释每个文件被修改的原因——不是重复 diff 内容，而是说明意图和决策理由。

---

## 3. `-m` 参数中 `\n` 的正确使用

当使用 `git commit -m "subject" -m "body"` 这种多 `-m` 形式时，Git 会在每个 `-m` 参数之间自动插入一个真正的换行符，这是推荐的用法。

但如果必须在一个 `-m` 参数内部使用 `\n` 来换行，**务必注意 shell 的转义规则**：在多数 shell（如 bash/zsh）中，`$'...'` 语法会解析 `\n` 为真正的换行符，而普通的双引号 `"..."` 不会——`\n` 会被当作字面字符串 `\n` 保留，导致提交信息中直接出现了 `\n` 文本而非实际换行。

简而言之：

- **推荐**：使用多个 `-m` 参数，每个 `-m` 对应一段逻辑独立的文本，由 Git 自动处理换行。
- **不推荐但可用**：使用 `$'...'` 语法包裹整个 commit message，让 shell 正确解析 `\n`。
- **禁止**：在普通双引号 `"..."` 内直接写 `\n` 然后期望它变成换行——它不会。

### Bad（双引号内 \n 不生效）

```bash
git commit -m "本次修改的主要内容描述。\n\n- path/to/file.kt：做了某修改，说明原因。\n- path/to/another.kt：做了另一修改，说明原因。"
# 提交信息中会原样出现 \n 字符串，不会换行
```

### Good（多 -m 参数，推荐）

```bash
git commit \
  -m "type(scope): 简短描述" \
  -m "本次修改的主要内容及要解决的问题。" \
  -m "- path/to/file.kt：做了某修改，说明原因。
- path/to/another.kt：做了另一修改，说明原因。"
```

### Good（$'...' 语法，备选）

```bash
git commit -m $'type(scope): 简短描述\n\n本次修改的主要内容及要解决的问题。\n\n- path/to/file.kt：做了某修改，说明原因。\n- path/to/another.kt：做了另一修改，说明原因。'
```

---

## 4. 示例

```
feat(auth): 添加基于 JWT 的登录认证功能

本次修改为系统引入了用户登录认证机制。之前系统缺少身份验证，
所有接口均为匿名访问，存在安全隐患。通过引入 JWT 令牌机制，
实现了无状态的用户认证。

- src/auth/jwt.rs: 新增 JWT 编码与解码逻辑，使用 HS256 算法签名
- src/auth/middleware.rs: 新增认证中间件，拦截请求并校验令牌有效性
- src/handlers/login.rs: 新增登录接口，验证用户名密码后签发令牌
- Cargo.toml: 添加 jsonwebtoken crate 依赖
```
